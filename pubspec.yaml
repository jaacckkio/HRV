name: vagal_hrv_camera
description: Camera-based HRV measurement module for Vagally Better
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.8.1
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  wakelock_plus: ^1.2.0
  permission_handler: ^12.0.1
  path_provider: ^2.1.0
  flutter_blue_plus: ^1.35.2
  share_plus: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
